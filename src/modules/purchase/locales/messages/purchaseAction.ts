import { defineMessages } from 'react-intl';

const prefix = 'purchase.action';

export const purchaseAction = defineMessages({
  modify: {id: `${prefix}.modify`},
  settle: {id: `${prefix}.settle`},
  add: {id: `${prefix}.add`},
});