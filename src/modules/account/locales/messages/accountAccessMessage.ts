import { defineMessages } from 'react-intl';

const prefix = 'account';

export const accountAccessMessage = defineMessages({
  greeting: { id: `${prefix}.access.message.greeting` },
  selection: { id: `${prefix}.access.message.selection` }
});