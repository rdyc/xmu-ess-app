import { Typography } from '@material-ui/core';
import * as React from 'react';
import { FormattedMessage } from 'react-intl';

export const NotFound = () => (
  <Typography>
    <FormattedMessage id="layout.page.notFound" />
  </Typography>
);