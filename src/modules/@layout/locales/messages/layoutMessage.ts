import { layoutAction, layoutField, layoutText, layoutTooltip } from './layoutAllMessage';

export const layoutMessage = {
  text: layoutText,
  tooltip: layoutTooltip,
  action: layoutAction,
  field: layoutField,
};
