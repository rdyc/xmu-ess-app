import { defineMessages } from 'react-intl';

const prefix = 'project.registration';

export const projectHourMessage = defineMessages({
  emptyProps: { id: `${prefix}.message.hour.emptyProps` },
  updateSuccess: { id: `${prefix}.message.hour.update.success` },
  updateFailure: { id: `${prefix}.message.hour.update.failure` },
});